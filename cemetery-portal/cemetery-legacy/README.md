# cemetery-legacy — reference only

Drop the **existing** cemetery app + database here so it can be studied and ported.
This whole folder is temporary and will be **deleted** once the cemetery portal is
rebuilt in the unified app.

```
cemetery-legacy/
  app/     <- existing cemetery app source (clone/upload/paste here)
  db/      <- existing cemetery DB: schema.sql (required) + data dump (helpful)
```

See `../KICKOFF.md` for the full brief and the decisions already made.
